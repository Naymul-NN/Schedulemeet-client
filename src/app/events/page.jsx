import Events from "@/components/events/Events";
import Layout from "../mainlayout";

const EventsPage = () => {
  return (
    <Layout>
      <Events />
    </Layout>
  );
};

export default EventsPage;
